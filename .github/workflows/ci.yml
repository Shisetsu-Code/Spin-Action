name: CI

on:
  push:
    branches: [main, lab/actions-validation]
  pull_request:

jobs:
  test:
    runs-on: ubuntu-latest
    timeout-minutes: 10
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-python@v5
        with:
          python-version: '3.12'
          cache: 'pip'
      - name: Install dependencies
        run: python -m pip install -r requirements.txt pytest
      - name: Compile
        run: python -m compileall -q tester_spin tests run.py
      - name: Unit tests
        run: python -m unittest discover -s tests -v
      - name: Function regressions and integration tests
        run: python -m pytest tests -q
